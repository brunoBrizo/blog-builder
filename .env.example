# -----------------------------------------------------------------------------
# Supabase (dev)
# -----------------------------------------------------------------------------
# Pooled connection (transaction pooler, port 6543) for runtime use (API, web).
# Use this for @blog-builder/db client at runtime.
DATABASE_URL="postgresql://postgres.<project-ref>:<password>@aws-<region>.pooler.supabase.com:6543/postgres"

# Direct connection (port 5432) used by drizzle-kit for migrations/studio.
# Required because pooled connections don't support DDL transactions reliably.
DIRECT_DATABASE_URL="postgresql://postgres.<project-ref>:<password>@aws-<region>.pooler.supabase.com:5432/postgres"

# Supabase REST / Auth (backend + admin only).
SUPABASE_URL="https://<project-ref>.supabase.co"
SUPABASE_SECRET_KEY="sb_secret_xxx"
SUPABASE_PUBLISHABLE_KEY="sb_publishable_xxx"

# -----------------------------------------------------------------------------
# API (apps/api) — mirrors apps/api/src/core/config/env.schema.ts
# -----------------------------------------------------------------------------
PORT=3001
NODE_ENV=development
LOG_LEVEL=info

# Throttling (defaults; optional overrides)
# THROTTLE_PUBLIC_READ_LIMIT=120
# THROTTLE_PUBLIC_READ_TTL_MS=60000
# THROTTLE_PUBLIC_WRITE_LIMIT=30
# THROTTLE_PUBLIC_WRITE_TTL_MS=60000
# THROTTLE_ADMIN_LIMIT=60
# THROTTLE_ADMIN_TTL_MS=60000

# Integrations (required for boot)
PERPLEXITY_API_KEY="pplx-xxx"
RESEND_API_KEY="re_xxx"

# Inngest (article generation pipeline). Empty in dev uses local dev server / env discovery; production requires both keys.
# INNGEST_EVENT_KEY=""
# INNGEST_SIGNING_KEY=""
# INNGEST_SERVE_PATH="/api/inngest"
# See docs/inngest-article-pipeline.md for replay, kill switch, and signing behavior.

# Article generation (Perplexity + Inngest). Defaults match env.schema.ts.
# GENERATION_PER_RUN_TOKEN_BUDGET=500000
# GENERATION_DAILY_USD_CEILING=500
# GENERATION_KILL_SWITCH=false
# GENERATION_DEFAULT_AUTHOR_ID="b1111111-1111-4111-8111-111111111111"
# PERPLEXITY_TIMEOUT_MS=120000
# PERPLEXITY_USD_PER_MTOKENS_PROMPT=3
# PERPLEXITY_USD_PER_MTOKENS_COMPLETION=15

# Shared secrets (generate strong random values)
CRON_SHARED_SECRET="dev-cron-secret"
REVALIDATE_SHARED_SECRET="dev-revalidate-secret"

# CORS: origin for apps/web (required in production)
CORS_ORIGIN_WEB="http://localhost:3000"

# Public web origin (no path) — API calls POST /api/revalidate here after auto-publish. Required in production.
# WEB_PUBLIC_ORIGIN="https://your-site.example"

# Scheduled topic queue refill (Perplexity). Optional overrides:
# GENERATION_SCHEDULER_TOPIC_SEED="Emerging topics in ..."
# GENERATION_TOPIC_QUEUE_MIN_DEPTH=3

# Optional: restrict cron routes by IP (comma-separated CIDRs; empty = no restriction)
# CRON_IP_ALLOWLIST=""
# TRUST_PROXY=false

# Optional: Sentry
# SENTRY_DSN=""
# SENTRY_TRACES_SAMPLE_RATE=1

# Optional: build metadata (injected in CI)
# GIT_SHA=""
# RELEASE_TAG=""
# BUILT_AT=""

# Optional: integration tests / dev-only probes
# ENABLE_TEST_ENDPOINTS=true

# -----------------------------------------------------------------------------
# Web (apps/web)
# -----------------------------------------------------------------------------
# Public API base URL including /api prefix (browser + server)
NEXT_PUBLIC_API_BASE_URL="http://localhost:3001/api"

# Same-origin URL for server-side calls to this app (ISR helper, local dev).
# NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Public site origin used by libs/seo (canonical URLs, hreflang, JSON-LD, OG).
# Must be the absolute origin without a trailing slash. In production this
# should be the deployed domain (e.g. https://blog.example.com).
# Fallbacks when unset: VERCEL_URL (prefixed with https://), then http://localhost:3000.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Server-only: same value as REVALIDATE_SHARED_SECRET on the API (for POST /api/revalidate)
# REVALIDATE_SHARED_SECRET="dev-revalidate-secret"
